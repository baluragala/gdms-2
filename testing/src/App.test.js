describe("All", () => {
  describe("set1", () => {
    it("should run1", () => {
      expect(2).toEqual(2);
    });

    it("should run2", () => {
      expect(2).toEqual(2);
    });

    it("should run2", () => {
      expect(2).toEqual(2);
    });
  });
  describe("set2", () => {
    it("should run1", () => {
      expect(2).toEqual(2);
    });

    it("should run2", () => {
      expect(2).toEqual(2);
    });

    it("should run2", () => {
      expect(2).toEqual(2);
    });
  });
});
